export const metadata = {
  title: "Sepet | Güngören FK",
  description: "Güngören FK mağaza sepeti.",
};

export default function SepetLayout({ children }: { children: React.ReactNode }) {
  return children;
}
