export const metadata = {
  title: "Ödeme | Güngören FK",
  description: "Güngören FK mağaza ödeme — iyzico ile güvenli ödeme.",
};

export default function OdemeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
